# Campus Connect Portal

A campus connectivity portal for students and staff.

## Getting started

1. Install dependencies for the client and server.
2. Configure environment variables in a local `.env` file.
3. Run the backend and frontend as needed for development.

## Project structure

- `client/` – frontend app
- `server/` – backend API
- `package.json` – root scripts
